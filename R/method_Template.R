parmsTemplate <- function(parmsString) {
  return(list())
}

fitTemplate <- function(xTrain, parms) {
  model <- NULL
  return(model)
}

predictTemplate <- function(model, initialConditions, nPred) {
  prediction <- matrix()
  return(prediction)
}

assimilateTemplate <- function(model, xTrain) {
  assimilation <- matrix()
  return(assimilation)
}

coefTemplate <- function(model) {
  return(matrix())
}
